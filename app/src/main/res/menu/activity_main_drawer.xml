<menu xmlns:android="http://schemas.android.com/apk/res/android">
		<group android:checkableBehavior="single">
				<item
						android:id="@+id/nav_check_new_version"
						android:icon="@drawable/icon_new_version"
						android:title="检查新版本"/>
		</group>
		<group>
				<item
						android:id="@+id/nav_exit_login"
						android:icon="@drawable/icon_exit_login"
						android:title="退出登录"/>
		</group>
</menu>